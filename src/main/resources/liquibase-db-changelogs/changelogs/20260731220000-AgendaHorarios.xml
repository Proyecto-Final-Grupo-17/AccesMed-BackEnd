<?xml version="1.0" encoding="UTF-8"?>
<databaseChangeLog
        xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
        xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
        xsi:schemaLocation="http://www.liquibase.org/xml/ns/dbchangelog
        http://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-latest.xsd">

    <changeSet id="20260731220000-added-table-AgendaHorarios" author="claude">
        <createTable tableName="agenda_horarios">
            <!-- ====== Columnas ====== -->
            <column name="id" type="uuid" defaultValueComputed="gen_random_uuid()">
                <constraints primaryKey="true" primaryKeyName="pk_agenda_horarios"/>
            </column>
            <column name="hora_desde" type="time">
                <constraints nullable="false"/>
            </column>
            <column name="hora_hasta" type="time">
                <constraints nullable="false"/>
            </column>
            <column name="fecha_limite_reserva" type="timestamptz">
                <constraints nullable="false"/>
            </column>
            <column name="esta_ocupada" type="boolean" defaultValueBoolean="false">
                <constraints nullable="false"/>
            </column>
            <!-- ====== Relaciones ====== -->
            <column name="agenda_dia_id" type="uuid">
                <constraints nullable="false" foreignKeyName="fk_agenda_horarios_agenda_dia" references="agenda_dia(id)"/>
            </column>
            <column name="prestacion_id" type="uuid">
                <constraints nullable="false" foreignKeyName="fk_agenda_horarios_prestacion" references="prestacion(id)"/>
            </column>
            <!-- ====== Columnas auditoría (incluye a la baja) ====== -->
            <column name="created_at" type="timestamptz">
                <constraints nullable="false"/>
            </column>
            <column name="updated_at" type="timestamptz"/>
            <column name="created_by" type="varchar(255)"/>
            <column name="updated_by" type="varchar(255)"/>
            <column name="deleted_at" type="timestamptz"/>
            <column name="deleted_by" type="uuid"/>
            <column name="deleted_reason" type="varchar(500)"/>
        </createTable>

        <sql>ALTER TABLE agenda_horarios ADD CONSTRAINT ck_agenda_horarios_horas CHECK (hora_desde &lt; hora_hasta);</sql>

        <sql>CREATE INDEX idx_agenda_horarios_fecha_limite_reserva ON agenda_horarios (fecha_limite_reserva);</sql>
        <sql>CREATE INDEX idx_agenda_horarios_agenda_dia ON agenda_horarios (agenda_dia_id);</sql>
        <sql>CREATE INDEX idx_agenda_horarios_prestacion ON agenda_horarios (prestacion_id);</sql>
        <rollback>
            <sql>ALTER TABLE agenda_horarios DROP CONSTRAINT IF EXISTS ck_agenda_horarios_horas;</sql>
            <sql>DROP INDEX IF EXISTS idx_agenda_horarios_fecha_limite_reserva;</sql>
            <sql>DROP INDEX IF EXISTS idx_agenda_horarios_agenda_dia;</sql>
            <sql>DROP INDEX IF EXISTS idx_agenda_horarios_prestacion;</sql>
        </rollback>
    </changeSet>

</databaseChangeLog>
