/**
 * Capa de aplicación: casos de uso ({@code <Entidad>App}) que orquestan cada flujo,
 * validando reglas de negocio contra los {@code Services} y coordinando el mapeo.
 */
package com.accesmed.backend.Application;
